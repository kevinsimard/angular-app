language: node_js

before_install:
    - npm install -g npm
    - npm install -g bower
    - npm install -g karma-cli

install:
    - npm install
    - bower install

before_script:
    - gulp www -env=testing

script:
    - karma start
