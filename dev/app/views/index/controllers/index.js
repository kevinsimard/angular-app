(function (angular) {
    'use strict';

    angular.module('app')
        .controller('index.index', controller);

    controller.$inject = [];

    function controller() {
        /* jshint validthis: true */
        var vm = this;
    }
})(angular);
