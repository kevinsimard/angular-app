(function (angular) {
    'use strict';

    angular.module('app')
        .controller('index.index', controller);

    controller.$inject = [];

    function controller() {
        var vm = this;
    }
})(angular);
